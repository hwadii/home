# no delay
set -s escape-time 0

# default shell
set-option -g default-shell /bin/zsh
# proper colors
set-option -g  default-terminal "tmux-256color"
set-option -ga terminal-overrides ",$TERM:Tc"
set -ga terminal-overrides '*:Ss=\E[%p1%d q:Se=\E[ q'

set -g base-index 1

# vi mode
set -g mode-keys vi
set -g history-limit 50000
set -g focus-events on

bind P paste-buffer
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-selection
bind-key -T copy-mode-vi r send-keys -X rectangle-toggle
bind-key -n C-S-Left swap-window -t -1\; select-window -t -1
bind-key -n C-S-Right swap-window -t +1\; select-window -t +1orld

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix
bind a last-window
bind C-p previous-window
bind C-n next-window

# mouse support
set-option -g mouse on
unbind -T copy-mode-vi MouseDragEnd1Pane

# open new panes and windows in CWD
bind-key c new-window -c "#{pane_current_path}"
bind-key % split-window -h -c "#{pane_current_path}"

# resizing (mouse also works)
unbind Left
bind -r Left resize-pane -L 5
unbind Right
bind -r Right resize-pane -R 5
unbind Down
bind -r Down resize-pane -D 5
unbind Up
bind -r Up resize-pane -U 5

# source-file "${HOME}/things/default.conf"
# set -g status-right '#{prefix_highlight} « #[fg=yellow]%a %d/%m %H:%M'
# set -g status-right '#{prefix_highlight} %a %d/%m %H:%M'
# set -g status-bg black
# set -g status-fg '#F8F8F2'

# Status bar.
set -g status-bg '#343d46'
set -g status-fg white
set -g status-left-length 40
set -g status-left '#[fg=yellow]#S ⧉ '
set -g status-left '#[fg=yellow]#S ☁️  '
set -g status-right "#{prefix_highlight}#[fg=yellow]%a %d/%m %H:%M"
set -g status-interval 60 # Default is 15.

# Automatically renumber window numbers on closing a pane (tmux >= 1.7).
set -g renumber-windows on

# Highlight active window.
# set -w -g window-status-current-bg red

run-shell "${HOME}/things/tmux-prefix-highlight/prefix_highlight.tmux"
bind r source-file ~/.tmux.conf

